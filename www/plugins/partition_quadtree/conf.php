<?
$partition_quadtree_active=true;

$partition_quadtree_depend=array("partition", "quadtree", "db");

$partition_quadtree_tags=new tags(array(
  "name"=>"Partition table into several parts AND build a quadtree on each subtable",
));

